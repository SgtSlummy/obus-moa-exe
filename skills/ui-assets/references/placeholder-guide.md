This reference explains how placeholder SVGs are used for missing card and key artwork in the OBus UI. The SVGs are stored in `assets/card-placeholder.svg` and `assets/key-placeholder.svg`. When rendering key or card cards, the template uses these paths as the image source. If you need custom artwork, replace the placeholder SVGs with your own images and update the `src` attribute in the HTML template accordingly.
