# Support script for key rotation

# This Python file lives in `scripts/key_rotate.py` and implements the rotation
# logic referenced above.  The script is intentionally lightweight so it can be
# run as a daily cron or invoked manually from a terminal.

# The script is provided as a support file under the `obus-model-registration`
# umbrella skill so that future agents can run it via the skill or the
# `cronjob` command.
