:root {
  --phi: 1.61803398875;
  --space-phi: 1.61803398875;
  --space-1: 3px;
  --space-2: 5px;
  --space-3: 8px;
  --space-4: 13px;
  --space-5: 21px;
  --space-6: 34px;
  --ink: #08090d;
  --ink-soft: #0d0f15;
  --ink-raised: #12151d;
  --ink-high: #181c25;
  --parchment: #f2eadb;
  --parchment-soft: #d8cdbc;
  --parchment-muted: #a99f90;
  --brass: #c5a35d;
  --brass-bright: #e2c37a;
  --verdigris: #63aaa2;
  --indigo: #8072ad;
  --danger-heritage: #d97978;
  --rule: color-mix(in srgb, var(--parchment) 15%, transparent);
  --rule-strong: color-mix(in srgb, var(--brass) 38%, var(--rule));
  --focus: 0 0 0 3px color-mix(in srgb, var(--verdigris) 52%, transparent);
  --shadow-resting: 0 1px 0 #ffffff08, 0 13px 34px #0000003d;
  --font-reading: "Segoe UI Variable", "Segoe UI", system-ui, sans-serif;
  --font-editorial: "Palatino Linotype", "Book Antiqua", Georgia, serif;
  --font-code: "Cascadia Mono", Consolas, ui-monospace, monospace;
  --control-min: 40px;
  --reading-measure: 76ch;
  --run-primary-fr: 1.618fr;
  --run-secondary-fr: 1fr;
  --splitter-size: 40px;

  /* Compatibility aliases for the existing shell. */
  --bg: var(--ink);
  --panel: var(--ink-raised);
  --panel2: var(--ink-high);
  --line: var(--rule);
  --text: var(--parchment);
  --muted: var(--parchment-muted);
  --gold: var(--brass-bright);
  --cyan: var(--verdigris);
  --violet: var(--indigo);
  --shadow: var(--shadow-resting);
  --focus-ring: var(--focus);
}

html {
  background: var(--ink);
  color-scheme: dark;
}

body {
  background-color: var(--ink);
  background-image:
    linear-gradient(90deg, transparent 0 20px, #c5a35d0a 21px, transparent 22px),
    linear-gradient(0deg, transparent 0 20px, #f2eadb08 21px, transparent 22px);
  background-size: 22px 22px;
  color: var(--parchment);
  font-family: var(--font-reading);
  line-height: 1.5;
  text-rendering: optimizeLegibility;
}

::selection {
  background: color-mix(in srgb, var(--brass) 42%, transparent);
  color: var(--parchment);
}

.shell {
  max-width: none;
  grid-template-columns: clamp(12rem, 15vw, 15.5rem) minmax(0, 1fr);
}

.side {
  border-color: var(--rule);
  background: color-mix(in srgb, var(--ink) 94%, var(--brass) 6%);
  backdrop-filter: none;
}

.brand {
  border-bottom: 1px solid var(--rule);
  margin-bottom: var(--space-4);
  padding-bottom: var(--space-5);
}

.brand h1,
.title h2,
.panel-head h3,
.dialog-head h3 {
  color: var(--parchment);
  font-family: var(--font-editorial);
  font-weight: 600;
  letter-spacing: -.015em;
}

.brand small,
.hint,
.title p {
  color: var(--parchment-muted);
}

.hint {
  min-width: 0;
  max-width: 100%;
  overflow-wrap: anywhere;
  word-break: break-word;
}

.actions > .hint {
  flex: 1 1 12rem;
}

.hint,
.metric span,
.row p,
.key-meta p,
.agent-info p,
.stage-meta,
.terminal-safety {
  font-size: .8125rem;
  line-height: 1.5;
}

.nav {
  gap: var(--space-2);
}

.nav button {
  min-block-size: var(--control-min);
  border: 1px solid transparent;
  border-radius: var(--space-3);
  color: var(--parchment-muted);
  line-height: 1.25;
}

.nav button:hover,
.nav button.active {
  border-color: var(--rule);
  background: var(--ink-high);
  color: var(--parchment);
}

.nav button.active {
  box-shadow: inset 3px 0 var(--brass);
}

main {
  padding: var(--space-5) clamp(var(--space-4), 2.4vw, var(--space-6)) var(--space-6);
}

.top {
  align-items: flex-start;
  gap: var(--space-5);
  margin-bottom: var(--space-5);
}

.top .layout-control,
.top .layout-control .hint,
.top .status {
  flex: 0 0 auto;
  white-space: nowrap;
}

.top .layout-control .select {
  min-width: 7.5rem;
}

.top > .actions,
.panel-head > .actions,
.terminal-block-head > .output-options,
.key-actions,
.row-actions {
  min-width: 0;
  gap: var(--space-3);
  flex-wrap: wrap;
}

.title {
  min-width: min(100%, 16rem);
}

.title h2 {
  font-size: clamp(1.6rem, 2.4vw, 2.35rem);
  line-height: 1.08;
}

.panel,
.terminal-block,
.shuffle-deck,
.integration,
.row,
.forge-card,
.agent-stage {
  border-color: var(--rule);
  background: var(--ink-raised);
  box-shadow: var(--shadow-resting);
}

.panel,
.terminal-block,
.shuffle-deck {
  border-radius: var(--space-4);
}

.panel-head,
.terminal-block-head,
.dialog-head,
.dialog-foot {
  min-width: 0;
  gap: var(--space-4);
  border-color: var(--rule);
}

.panel-head {
  min-block-size: 55px;
  padding: var(--space-4) var(--space-5);
  flex-wrap: wrap;
}

.panel-head > :first-child,
.panel-head h3,
.panel-head .hint {
  min-width: 0;
  overflow-wrap: anywhere;
}

.panel-head h3 {
  font-size: 1rem;
  line-height: 1.25;
}

.panel-body {
  min-width: 0;
  padding: var(--space-5);
}

.button,
.select,
.terminal-tab,
button:not(.agent-face-button),
input:not([type="checkbox"]):not([type="radio"]),
select,
textarea {
  min-block-size: var(--control-min);
}

.button,
.select,
.terminal-tab,
input:not([type="checkbox"]):not([type="radio"]),
select {
  border-color: var(--rule);
  border-radius: var(--space-3);
  background: var(--ink-high);
  color: var(--parchment);
}

.button {
  padding: var(--space-3) var(--space-4);
  font-weight: 600;
}

.button.primary {
  border: 1px solid var(--brass);
  background: var(--brass);
  color: #221b0f;
  font-weight: 700;
}

.button:hover,
.select:hover,
.terminal-tab:hover {
  border-color: var(--brass);
  transform: none;
}

.button:focus-visible,
.select:focus-visible,
.terminal-tab:focus-visible,
input:focus-visible,
select:focus-visible,
textarea:focus-visible,
[tabindex]:focus-visible {
  outline: 0;
  box-shadow: var(--focus);
}

.status,
.badge {
  border-color: var(--rule);
  background: var(--ink-soft);
  color: var(--parchment-soft);
}

.badge {
  margin-top: var(--space-2);
}

.hermes-composer {
  min-block-size: 63px;
  border-color: var(--rule-strong);
  border-radius: var(--space-4);
  background: var(--ink);
  box-shadow: inset 0 1px #ffffff08;
}

.hermes-composer:focus-within {
  border-color: var(--verdigris);
  box-shadow: var(--focus), inset 0 1px #ffffff0a;
  transform: none;
}

.hermes-composer textarea {
  min-width: 0;
  max-width: 100%;
  min-block-size: var(--control-min);
  max-block-size: min(45vh, 34rem);
  padding: var(--space-2) 0;
  resize: vertical;
  color: var(--parchment);
  font-family: var(--font-code);
  line-height: 1.55;
  overflow-wrap: anywhere;
  word-break: break-word;
  scrollbar-gutter: stable;
}

.hermes-composer textarea::placeholder,
input::placeholder,
textarea::placeholder {
  color: color-mix(in srgb, var(--parchment-muted) 82%, transparent);
}

textarea::-webkit-scrollbar,
.result::-webkit-scrollbar,
.code-output::-webkit-scrollbar {
  width: 8px;
  height: 8px;
}

textarea::-webkit-scrollbar-button,
.result::-webkit-scrollbar-button,
.code-output::-webkit-scrollbar-button {
  display: none;
  width: 0;
  height: 0;
}

textarea::-webkit-resizer {
  border: 0;
  background: linear-gradient(135deg, transparent 55%, var(--rule-strong) 56% 62%, transparent 63%);
}

:where(.result, .code-output, .terminal-result, .agent-context-text, #workspace-file, #room-deliberation, #room-decision) {
  min-block-size: 6rem;
  max-block-size: min(72vh, 50rem);
  overflow: auto;
  resize: vertical;
  scrollbar-gutter: stable both-edges;
  border: 1px solid var(--rule);
  border-radius: var(--space-3);
  background: var(--ink);
  color: var(--parchment-soft);
  font-family: var(--font-code);
  line-height: 1.55;
  overflow-wrap: anywhere;
}

.terminal-workbench {
  display: grid;
  grid-template-columns: minmax(0, var(--run-primary-fr)) var(--splitter-size) minmax(17rem, var(--run-secondary-fr));
  gap: var(--space-3);
  align-items: start;
}

.pane-splitter {
  position: relative;
  align-self: stretch;
  min-block-size: 12rem;
  border: 1px solid transparent;
  border-radius: 999px;
  background: transparent;
  cursor: col-resize;
  touch-action: none;
}

.pane-splitter::before {
  content: "";
  position: absolute;
  inset-block: var(--space-5);
  left: 50%;
  width: 1px;
  border-radius: 999px;
  background: var(--rule-strong);
  transform: translateX(-50%);
}

.pane-splitter:hover,
.pane-splitter:focus-visible {
  border-color: var(--brass);
  background: color-mix(in srgb, var(--brass) 9%, transparent);
  outline: 0;
  box-shadow: var(--focus);
}

.terminal-stack,
.terminal-rail {
  min-width: 0;
  gap: var(--space-4);
}

.terminal-toolbar,
.terminal-command,
.terminal-output {
  background: var(--ink-soft);
}

.terminal-block-head {
  min-block-size: 44px;
  padding: var(--space-3) var(--space-4);
  color: var(--parchment-muted);
  font-family: var(--font-code);
  flex-wrap: wrap;
}

.terminal-block-head strong,
.context,
.connection-value,
.aui-summary strong {
  color: var(--verdigris);
}

.terminal-tab {
  padding: var(--space-3) var(--space-4);
}

.terminal-tab.active {
  border-color: var(--brass);
  background: color-mix(in srgb, var(--brass) 12%, var(--ink-high));
  color: var(--parchment);
}

.workspace-editor {
  display: block;
  inline-size: 100%;
  min-block-size: 18rem;
  max-block-size: min(72vh, 50rem);
  resize: vertical;
  overflow: auto;
  border: 1px solid var(--verdigris);
  border-radius: var(--space-3);
  background: var(--ink);
  color: var(--parchment-soft);
  font: 0.82rem/1.55 var(--font-code);
  padding: var(--space-3);
  tab-size: 2;
}

.workspace-editor:focus-visible {
  outline: 0;
  box-shadow: var(--focus);
}

.workspace-risk-approval {
  color: var(--brass);
  font-size: 0.78rem;
}

.terminal-session-strip {
  display: flex;
  min-width: 0;
  padding: var(--space-2) var(--space-4);
  border-block-end: 1px solid var(--rule);
  background: var(--ink-high);
}

.terminal-session-tabs {
  display: flex;
  min-width: 0;
  gap: var(--space-2);
  overflow-x: auto;
  scrollbar-width: thin;
}

.terminal-session-tab-wrap {
  display: inline-flex;
  min-width: 0;
  border: 1px solid var(--rule);
  border-radius: var(--space-2);
  background: var(--ink-soft);
}

.terminal-session-tab {
  display: inline-flex;
  min-width: 8rem;
  max-width: 15rem;
  align-items: center;
  justify-content: space-between;
  gap: var(--space-3);
  border: 0;
  border-radius: calc(var(--space-2) - 1px) 0 0 calc(var(--space-2) - 1px);
  background: transparent;
  color: var(--parchment-muted);
  cursor: pointer;
  font: 0.78rem/1.25 var(--font-code);
  padding: var(--space-2) var(--space-3);
}

.terminal-session-tab span,
.terminal-session-tab small {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.terminal-session-tab small {
  color: var(--parchment-muted);
  font-size: 0.68rem;
}

.terminal-session-tab.active {
  background: color-mix(in srgb, var(--verdigris) 14%, var(--ink-soft));
  color: var(--parchment);
}

.terminal-session-tab.active small {
  color: var(--verdigris);
}

.terminal-session-tab:focus-visible,
.terminal-session-close:focus-visible {
  position: relative;
  z-index: 1;
  outline: 0;
  box-shadow: var(--focus);
}

.terminal-session-close {
  inline-size: 1.9rem;
  border: 0;
  border-inline-start: 1px solid var(--rule);
  border-radius: 0 calc(var(--space-2) - 1px) calc(var(--space-2) - 1px) 0;
  background: transparent;
  color: var(--parchment-muted);
  cursor: pointer;
  font: 1.1rem/1 var(--font-ui);
}

.terminal-session-close:hover {
  background: color-mix(in srgb, var(--danger) 15%, transparent);
  color: var(--danger);
}

.terminal-session-empty {
  color: var(--parchment-muted);
  font: 0.76rem/1.4 var(--font-code);
  padding: var(--space-2) 0;
}

.metric {
  min-width: 0;
  border-color: var(--rule);
  border-radius: var(--space-3);
  background: var(--ink-soft);
}

.metric strong,
.metric span,
.row h4,
.row p,
.key-meta,
.agent-info,
.stage-meta,
.connection-value,
code,
pre {
  min-width: 0;
  overflow-wrap: anywhere;
}

.room-workspace {
  grid-template-columns: minmax(0, 1.618fr) minmax(17rem, 1fr);
  gap: var(--space-5);
}

.adjustable-workspace {
  display: grid;
  grid-template-columns: minmax(0, var(--pane-primary-fr, 1.618fr)) var(--splitter-size) minmax(16rem, var(--pane-secondary-fr, 1fr));
  gap: var(--space-3);
  align-items: start;
}

.studio-secondary {
  display: grid;
  min-width: 0;
  gap: var(--space-4);
}

#rooms-workspace > :first-child {
  min-height: 20rem;
  padding: var(--space-5);
  border: 1px solid var(--rule);
  border-radius: var(--space-4);
  background: radial-gradient(circle at 50% 0, color-mix(in srgb, var(--brass) 7%, transparent), transparent 58%), var(--ink-soft);
}

#rooms-workspace #room-list > .empty {
  display: grid;
  min-height: 14rem;
  place-items: center;
  padding: var(--space-5);
  text-align: center;
  color: var(--parchment-muted);
}

#rooms-workspace #room-list > .empty::before {
  content: "✦";
  display: block;
  margin-bottom: var(--space-3);
  color: var(--brass-bright);
  font-family: var(--font-editorial);
  font-size: 2rem;
}

.page[data-page-panel="runs"] > .panel > .panel-body {
  display: grid;
  grid-template-columns: minmax(18rem, .618fr) minmax(0, 1fr);
  align-items: start;
  gap: var(--space-5);
}

.page[data-page-panel="runs"] #run-list {
  min-width: 0;
}

.page[data-page-panel="runs"] #run-receipt-detail {
  min-width: 0;
  min-height: 20rem;
  margin: 0 !important;
}

.terminal-history small {
  display: block;
  margin-top: var(--space-2);
  font-variant-numeric: tabular-nums;
  word-break: normal;
  overflow-wrap: normal;
}

#settings-import-preview {
  grid-row: 1 / 3;
  min-width: 0;
  max-height: 18rem;
  overflow: auto;
  word-break: break-word;
}

.aui-summary {
  display: flex;
  align-items: center;
  gap: var(--space-3);
}

.aui-action {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  align-items: start;
  gap: var(--space-4);
  background: var(--ink-soft);
}

.aui-action > span {
  display: grid;
  min-width: 0;
  gap: var(--space-1);
}

.aui-action strong,
.aui-action small {
  display: block;
  overflow-wrap: anywhere;
}

.aui-action kbd {
  align-self: start;
  white-space: nowrap;
}

.guide {
  grid-template-columns: repeat(auto-fit, minmax(min(100%, 12rem), 1fr));
}

.guide div {
  display: grid;
  grid-template-columns: 2ch minmax(0, 1fr);
  column-gap: var(--space-2);
  align-items: start;
}

.guide strong {
  margin: 0;
}

.key-grid.key-shuffle-deck {
  grid-template-columns: repeat(auto-fill, minmax(min(100%, 19rem), 1fr));
  gap: var(--space-4);
}

.grid.tarot-shuffle-deck {
  grid-template-columns: repeat(auto-fill, minmax(min(100%, 13rem), 1fr));
  gap: var(--space-4);
}

.key-shuffle-deck .shuffle-card {
  --shuffle-tilt: 0deg;
  animation: none;
  transform: none;
}

.key-shuffle-deck .shuffle-card:hover,
.key-shuffle-deck .shuffle-card:focus-within {
  transform: translateY(-2px);
}

.key-shuffle-deck .key-card {
  gap: var(--space-3);
}

.key-shuffle-deck .key-art {
  object-fit: contain;
  background: var(--ink);
}

.key-shuffle-deck .key-meta,
.key-shuffle-deck .key-actions {
  position: static;
  z-index: auto;
}

.key-actions {
  align-items: center;
  padding-top: var(--space-3);
}

.key-actions a.button {
  display: inline-flex;
  align-items: center;
  min-height: var(--control-min);
}

.agent-card-face {
  min-block-size: var(--control-min);
}

.card-art,
.key-art,
.emblem img {
  max-width: 100%;
}

.guide {
  grid-template-columns: repeat(auto-fit, minmax(min(100%, 12rem), 1fr));
  gap: var(--space-3);
}

.guide div {
  border-color: var(--rule);
  border-radius: var(--space-3);
  background: var(--ink-soft);
}

.dialog-head,
.dialog-body,
.dialog-foot,
dialog {
  background: var(--ink-raised);
  color: var(--parchment);
}

.row,
.deliberation-message,
.forge-card,
.integration {
  min-width: 0;
  overflow-wrap: anywhere;
}

.mobile-nav-drawer {
  display: block;
}

.mobile-control-drawer {
  display: block;
  flex: 1 1 42rem;
  min-width: 0;
}

.mobile-drawer-summary {
  display: none;
}

.mobile-nav-drawer:not([open]) > .nav {
  display: grid;
}

.mobile-control-drawer:not([open]) > .actions {
  display: flex;
}

.mobile-control-drawer > .actions {
  justify-content: flex-end;
}

#settings-import-preview {
  grid-template-columns: minmax(16rem, .75fr) minmax(0, 1.25fr);
  align-items: start;
}

#settings-import-content {
  grid-column: 2;
  grid-row: 1 / 3;
  width: 100%;
  margin: 0;
}

#settings-import-apply {
  grid-column: 1;
  justify-self: start;
}

.page-jump-top {
  display: none;
}

@media (max-width: 960px) {
  .terminal-workbench,
  .room-workspace,
  .adjustable-workspace {
    grid-template-columns: minmax(0, 1fr);
  }

  .terminal-rail {
    position: static;
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .pane-splitter {
    display: none;
  }

  .page[data-page-panel="runs"] > .panel > .panel-body {
    grid-template-columns: minmax(0, 1fr);
  }
}

@media (max-width: 720px) {
  .shell {
    display: block;
    grid-template-columns: minmax(0, 1fr);
    min-width: 0;
  }

  .side {
    position: sticky;
    z-index: 8;
    top: 0;
    width: 100%;
    height: auto;
    padding: var(--space-2) var(--space-3);
    border-right: 0;
    border-bottom: 1px solid var(--rule);
  }

  .brand {
    display: none;
  }

  .mobile-nav-drawer,
  .mobile-control-drawer {
    display: block;
  }

  .mobile-drawer-summary {
    display: flex;
    min-height: var(--control-min);
    align-items: center;
    justify-content: space-between;
    gap: var(--space-3);
    padding: var(--space-3) var(--space-4);
    border: 1px solid var(--rule);
    border-radius: var(--space-3);
    background: var(--ink-high);
    color: var(--parchment);
    cursor: pointer;
    list-style: none;
  }

  .mobile-drawer-summary::-webkit-details-marker {
    display: none;
  }

  .mobile-drawer-summary::after {
    content: "⌄";
    color: var(--brass-bright);
  }

  details[open] > .mobile-drawer-summary::after {
    content: "⌃";
  }

  .mobile-nav-drawer:not([open]) > .nav,
  .mobile-control-drawer:not([open]) > .actions {
    display: none;
  }

  .nav {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: var(--space-2);
    margin-top: var(--space-2);
    overflow-x: visible;
  }

  .nav button {
    min-width: 0;
    padding-inline: var(--space-4);
    font-size: .75rem;
    text-align: left;
  }

  main {
    width: 100%;
    padding: var(--space-4);
  }

  .top {
    display: grid;
    grid-template-columns: minmax(0, 1fr);
  }

  .mobile-control-drawer > .actions {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    margin-top: var(--space-2);
  }

  .terminal-toolbar {
    display: grid;
    grid-template-columns: minmax(0, 1fr);
  }

  .terminal-tabs {
    display: grid;
    width: 100%;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: var(--space-2);
    overflow: visible;
  }

  .terminal-tab {
    min-width: 0;
    padding-inline: var(--space-2);
    white-space: normal;
    text-align: center;
  }

  .page[data-page-panel="settings"] .row {
    grid-template-columns: minmax(0, 1fr);
  }

  .page[data-page-panel="settings"] .row > * {
    min-width: 0;
    max-width: 100%;
  }

  #settings-import-preview {
    display: grid;
    max-height: none;
    overflow: visible;
  }

  #settings-import-content,
  #settings-import-apply {
    grid-column: 1 !important;
    grid-row: auto !important;
    width: 100%;
  }

  .page-jump-top:not([hidden]) {
    position: fixed;
    z-index: 12;
    right: var(--space-4);
    bottom: var(--space-4);
    display: inline-flex;
    align-items: center;
    min-height: var(--control-min);
    border-color: var(--brass);
    background: var(--ink-high);
    box-shadow: var(--shadow-resting);
  }

  .top > .actions > *,
  .mobile-control-drawer > .actions > * {
    width: 100%;
    min-width: 0;
    max-width: 100%;
  }

  .top .status,
  .top .toggle {
    justify-content: center;
    white-space: normal;
  }

  .top,
  .panel-head,
  .terminal-block-head {
    display: grid;
    grid-template-columns: minmax(0, 1fr);
    align-items: stretch;
  }

  .mobile-control-drawer > .actions,
  .panel-head > .actions,
  .output-options,
  .composer-foot .actions,
  .row-actions,
  .key-actions {
    width: 100%;
  }

  .terminal-rail,
  .grid,
  .grid.tarot-shuffle-deck,
  .key-grid.key-shuffle-deck,
  .integration-grid,
  .connection-grid,
  .form-grid {
    grid-template-columns: minmax(0, 1fr);
  }

  .button,
  .select,
  .terminal-tab {
    max-width: 100%;
  }
}

@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    scroll-behavior: auto !important;
  }

  .button,
  .panel,
  .terminal-block,
  .shuffle-card,
  .agent-face-button,
  .agent-face-spark,
  .agent-icon-stream {
    animation: none !important;
    transition: none !important;
  }
}
