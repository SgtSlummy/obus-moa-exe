# Build contexts are allowlisted because this checkout also contains multi-GB
# Windows packages, virtual environments, and generated UI distributions.
**

!requirements.txt
!requirements-headless.txt
!pytest.ini

!backend/
!backend/**

# Keep the legacy test-only Dockerfile usable without admitting the full suite.
!tests/
!tests/test_deliberate.py
